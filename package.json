{
  "name": "modscape",
  "version": "2.4.0",
  "description": "Modscape: A YAML-driven data modeling visualizer CLI",
  "repository": {
    "type": "git",
    "url": "https://github.com/yujikawa/modscape.git"
  },
  "type": "module",
  "bin": {
    "modscape": "./src/index.js"
  },
  "files": [
    "src",
    "visualizer-dist",
    "visualizer/public/favicon.svg",
    "package.json",
    "README.md"
  ],
  "scripts": {
    "build-ui": "cd visualizer && VITE_SINGLE_FILE=1 npm run build && rm -rf ../visualizer-dist && mv dist ../visualizer-dist",
    "dev": "node src/index.js dev",
    "test:cli": "playwright test tests/import-dbt.spec.ts",
    "test:e2e": "cp tests/fixtures/test-model.yaml tests/fixtures/test-model-runtime.yaml && playwright test",
    "test:all": "npm run build-ui && npm run test:cli",
    "test:update": "npm run build-ui && sleep 1 && npm run test:e2e -- --update-snapshots"
  },
  "dependencies": {
    "@inquirer/prompts": "^8.3.0",
    "chokidar": "^5.0.0",
    "commander": "^14.0.3",
    "dagre": "^0.8.5",
    "express": "^5.2.1",
    "js-yaml": "^4.1.1",
    "open": "^11.0.0",
    "ws": "^8.18.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
