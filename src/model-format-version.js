// Single source of truth for the model.yaml format version.
// Bump this when the model.yaml format changes (new fields, removed fields, structural changes).
// See MODEL_FORMAT_CHANGELOG.md for the history of changes.
export const MODEL_FORMAT_VERSION = '2.1.0';
